[build]
builder = "DOCKERFILE"
dockerfilePath = "artifacts/discord-bot/Dockerfile"

[deploy]
startCommand = "pnpm --filter @workspace/discord-bot run dev"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
